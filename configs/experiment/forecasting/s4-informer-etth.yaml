# @package _global_
defaults:
  - /pipeline: informer2  # informer
  - /model: s4
  - override /dataset: etth

#dataset:
#  variant: 0  # 0 | 1
#  features: 'S'  # S| M  multivariate
#  size:
#    - 384
#    - 720
#    - 720

trainer:
  max_epochs: 20 # 10

loader:
  batch_size: 50

model:
  dropout: 0.25
  n_layers: 2
  d_model: 128

optimizer:
  lr: 0.01
