# @package _global_
defaults:
  - /pipeline: informer-lm
  - /model: s4
  - override /dataset: etth-lm

dataset:
  variant: 0  # 0 | 1
  features: 'S'  # S| M  multivariate
#  size:
#    - 384
#    - 96
#    - 96

model:
  dropout: 0.25
  n_layers: 2
  d_model: 128

trainer:
  max_epochs: 100

loader:
  batch_size: 50


optimizer:
  lr: 0.01
