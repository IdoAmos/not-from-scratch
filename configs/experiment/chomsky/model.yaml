# @package _global_
defaults:
  - /pipeline: chomsky

model:
  name: transformer # lstm # rnn

decoder:
  _name_: sequence_nonelinear
  mode: pool

optimizer:
  lr: 0.001
  weight_decay: 0.0

trainer:
  max_epochs: 1000000

train:
  seed: 2222
